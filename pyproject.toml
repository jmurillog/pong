[project]
name = "pong-ppo-proposal"
version = "0.1.0"
description = "Reproducible PPO-on-Pong pipeline with human baseline comparison."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
